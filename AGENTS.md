# AGENTS.md

このディレクトリは、`bin/` を PATH に追加して日常的に使う個人用 CLI スクリプト集です。
AGENTS.md は人間向け README の代替ではなく、エージェントがこの場所で安全に作業するための実務ルールだけを書きます。

## 作業前に見るもの

- `README.md`: 利用者向けの説明と公開済みスクリプト一覧。
- `bin/`: PATH から直接呼び出される実行ファイル。
- `scripts/<name>/`: スクリプトごとの README、設定例、補助ファイル。
- 既存スクリプト: shebang、help、エラー処理、出力形式、命名の雰囲気を合わせる。
- 変更対象の `--help`: 既存のユーザーインターフェースを壊さない。

## このディレクトリの前提

- `bin/<command>` は、どの作業ディレクトリからでも直接呼び出せる単体コマンドとして扱う。
- `scripts/<command>/` には、そのコマンド専用の README、例、補助資料を置く。
- `scripts/<command>/config/` には、repo 管理する設定ファイル、テンプレート、サンプルを置く。
- 実行ファイル名は短く、用途が分かる名前にする。拡張子は、利用時に自然でなければ付けない。
- 新規スクリプトには実行権限を付ける。例: `chmod +x bin/<command>`
- 秘密情報、トークン、個別環境の認証情報は置かない。
- 個人用ツールなので、過度なフレームワーク化や共通化より、読めて壊れにくい実装を優先する。

## レイアウト

```text
shellarium/
  bin/                 PATH に追加する実行ファイル
  scripts/<command>/   個別 README、補助ファイル
    config/            設定ファイル、テンプレート、サンプル
  README.md            全体の導入と一覧
  AGENTS.md            エージェント向け作業ルール
```

## Bash スクリプトの基本

- shebang を必ず付ける。通常は `#!/usr/bin/env bash` を使う。
- 原則として冒頭で次を有効にする。

  ```bash
  set -euo pipefail
  ```

- 引数不備、依存コマンド不足、入力形式エラーは、分かりやすいメッセージを stderr に出して非 0 で終了する。
- 通常結果は stdout、ログ・警告・エラー・進捗は stderr に出す。
- 外部コマンド依存がある場合は、冒頭コメントまたは `--help` に書く。必須依存は `command -v` で確認する。
- 一時ファイルや一時ディレクトリが必要な場合は `mktemp` を使い、必要なら `trap` で後始末する。
- パス、空白、改行を含む入力を想定し、変数展開は基本的にクォートする。

## 安全境界

- 破壊的な処理はデフォルトで実行しない。`--dry-run`、`--yes`、確認プロンプトなど、安全側の操作を用意する。
- ホームディレクトリ、ワークスペース全体、ルート配下に対する広すぎる再帰削除・一括変更は禁止。
- `rm -rf`、`find -delete`、上書きリダイレクト、権限変更、ネットワーク送信を追加する場合は、対象範囲と失敗時の挙動を明示する。
- ユーザーの既存ファイルを変更するスクリプトでは、可能ならバックアップ、dry-run、対象一覧表示のいずれかを用意する。

## CLI と help

各スクリプトは、可能なら `-h` / `--help` を受け付ける。

```text
Usage:
  command [options] <args>

Options:
  -h, --help  Show help

Examples:
  command example
```

- help には、よく使う例、入力形式、危険なオプション、主な依存コマンドを書く。
- オプション名は既存スクリプトと一貫させる。短い別名は本当に頻用するものだけにする。
- 既存コマンドの挙動を変える場合は、後方互換性を優先し、全体 README と個別 README の例も更新する。

## 変更後の確認

- Bash の構文確認: `bash -n bin/<command>`
- help の確認: `./bin/<command> --help`
- 代表的な正常系と異常系を 1 つ以上実行する。
- 新規・変更した公開コマンドは、必要に応じて `README.md` と `scripts/<command>/README.md` を更新する。

## PATH 前提

このディレクトリの `bin/` を PATH に追加する例:

```bash
export PATH="/path/to/shellarium/bin:$PATH"
```

実際のパスは、このリポジトリを配置した場所に合わせて置き換えます。
シェル起動時に常に使う場合は、利用中のシェル設定ファイルに追記します。
